Enter file contents here
#include<stdio.h>
#include<stdlib.h>

intmain()
{
	int i, num, fact=1;
	
	printf("정수 입력 : ");
	scanf("%d", &num);

	{
	for(i=1 ; i<=num ; i++)
	}
		fact*=i;
	printf("%d! = %d\n", num, fact);

	system("pause");
	return 0;
}
